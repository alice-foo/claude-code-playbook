---
description: 汎用的なプロジェクト初期化。言語・フレームワークに依存しない共通設定（CLAUDE.md、README.md、プロジェクト構造）を自動生成。質問なしで即座に適用。
argument-hint: "[省略可]"
---

# 汎用プロジェクト初期化

このスキルは、言語やフレームワークの種別に関わらず、Claude Code プロジェクトとして共通する汎用的な初期設定を自動生成します。質問は一切なく、デフォルト値で即座に適用されます。

## 行動指針

以下の設計原則に従って行動してください：

- **計画先行の開発**: コードを書く前に必ず計画を立てる
- **簡潔な設定ファイル**: プロジェクトのルール・コマンド・構造を CLAUDE.md に集約する
- **段階的な実装**: 一度にすべてを作らず、フェーズごとに進める
- **毎回のビルド検証**: 各フェーズでビルド・テストが通ることを確認してから次に進む

## 生成される設定ファイル

### 1. CLAUDE.md
プロジェクトルートに生成されます。以下を含みます：

```markdown
# プロジェクト名

## プロジェクト概要
[プロジェクトの目的・概要を記述。ユーザーが後で編集]

## 開発原則

- **計画先行**: コードを書く前に設計を立てる
- **段階的実装**: 機能を小分けにしてフェーズ分けする
- **毎回の検証**: 各フェーズでビルド・テスト確認後、次のフェーズへ
- **簡潔さ**: 過度な最適化や未来の仕様を想定した設計は避ける

## ビルド・テスト

### ビルドコマンド
[言語に応じて後で編集。例: `npm run build`, `dotnet build`, など]

### テストコマンド
[言語に応じて後で編集。例: `npm test`, `dotnet test`, など]

## コーディング規約

### 言語に依存しない共通ルール

- **ファイル名**: プロジェクトの慣例に従う（snake_case / camelCase / PascalCase）
- **ディレクトリ構成**: 関連する機能・モジュール単位でグループ化
- **コメント**: 「なぜこうしたのか」を記述。自明な処理にはコメント不要
- **エラーハンドリング**: ユーザー入力・外部 API は必ずバリデーション。内部コード（フレームワーク保証）は信頼する
- **テスト**: 単体テストは主要機能から。複雑な条件分岐は特に手厚く

### 言語別コーディング規約
[言語に応じて後で追記。async/await、nullable 型、error handling パターン、etc.]

## プロジェクト構造

[プロジェクト特有のディレクトリ構成を後で編集]

推奨される一般的なレイアウト：

```
project-root/
├── src/          # ソースコード
├── tests/        # テストコード
├── docs/         # ドキュメント
├── .github/      # GitHub 設定（CI/CD など）
├── CLAUDE.md     # このファイル
├── README.md     # プロジェクト説明
└── .gitignore    # Git 除外ルール
```

## フェーズ分けと進め方

### フェーズ 1: 基本構造の構築

[実装計画を記述。各フェーズはコンテキストの 50% 以内で完了する粒度に分ける]

### フェーズ 2: 機能追加

[以降、フェーズを追加]

## その他

- **言語**: [後で編集]
- **フレームワーク**: [後で編集]
- **重要な依存関係**: [後で編集]
```

### 2. README.md
プロジェクトルートに生成されます。以下を含みます：

```markdown
# プロジェクト名

[プロジェクトの簡潔な説明]

## 特徴

- 特徴1
- 特徴2

## 環境構築

### 前提条件

- [言語・ランタイム]
- [その他の依存関係]

### セットアップ手順

1. リポジトリをクローン
   ```
   git clone <repository-url>
   cd <project-name>
   ```

2. 依存関係をインストール
   ```
   [言語に応じたコマンド]
   ```

3. ビルド
   ```
   [ビルドコマンド]
   ```

## 実行方法

```
[実行コマンド]
```

## テスト

```
[テストコマンド]
```

## プロジェクト構造

詳細は [CLAUDE.md](./CLAUDE.md) を参照してください。

## 開発ガイド

[CLAUDE.md](./CLAUDE.md) に開発原則、コーディング規約、フェーズごとの実装計画を記述しています。

## ライセンス

[MIT / Apache 2.0 / など。後で編集]

## 問い合わせ

[連絡先・リンク。後で編集]
```

### 3. .gitignore
一般的な除外ルール（言語非依存）を含みます：

```
# IDE / エディタ
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ビルド・キャッシュ
build/
dist/
.cache/
*.o
*.a
*.so
*.dylib
*.dll

# 依存関係
node_modules/
vendor/
packages/
.gradle/

# 環境
.env
.env.local
.env.*.local
*.log

# Python
.venv/
venv/
__pycache__/
*.pyc

# その他
tmp/
temp/
*.bak
```

### 4. .claude/memory/
メモリシステムディレクトリを作成。MEMORY.md インデックスを生成：

```markdown
# メモリインデックス

このディレクトリはプロジェクト固有の情報を永続化します。

## メモリの種類

- **user**: ユーザーのロール・目標・知識
- **feedback**: 開発時の教訓・方針
- **project**: 進行中のプロジェクト情報
- **reference**: 外部リソースへのポインタ

詳細は Claude Code のメモリシステムのドキュメントを参照。
```

## 生成手順

このスキルが実行されたら、現在のディレクトリに以下のファイルを直接生成してください：

1. `CLAUDE.md` — 上記テンプレートの内容で生成
2. `README.md` — 上記テンプレートの内容で生成
3. `.gitignore` — 上記の汎用除外ルールで生成
4. `.claude/memory/MEMORY.md` — 上記のメモリインデックスで生成

生成後、ユーザに以下を案内してください：
1. `CLAUDE.md` を編集して、プロジェクト概要を記述する
2. `README.md` を編集して、プロジェクト説明を追加する
3. 言語・フレームワークに応じた設定を追加する

## 注意事項

このスキルは汎用的なテンプレートを生成するのみです。プロジェクト固有の設定（技術スタック、コーディング規約、フェーズ計画など）は、生成後にユーザーが編集・カスタマイズしてください。

$ARGUMENTS
